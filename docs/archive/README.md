# Archive

Kept, not deleted — these were tracked in git, so someone put them here
deliberately. Moved out of the repository root because none of them belong to
this application.

| File | What it is |
|---|---|
| `7. DesignReview AI — AI UX Critique.txt` | A product brief for an unrelated project. Not part of Trippy's Mehfill. |
| `9. CareRoute — Responsible AI Navig.txt` | Likewise. |
| `test-site.js` | An ad-hoc Playwright script predating the E2E work. Superseded by `e2e/smoke.mjs` (29 checks) and `e2e/journey.mjs` (29 checks), which are wired into `npm run e2e`. Kept for reference. |

Delete them if you are confident they are not wanted — `git rm` and they are
still recoverable from history.
